# Release Naming Guideline

- When creating a GitHub release or version release, set the release title to ONLY the version number (for example, `v0.9.0`).
- Do NOT add descriptive subtitles, features, or summaries to the release title.
